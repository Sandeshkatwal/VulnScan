# Dependency Review

Use `.\.venv311\Scripts\python.exe scripts\check_dependencies.py` to review Python and dashboard dependency hygiene for Public Beta.

The check is intentionally advisory except for missing required dependency files. It does not auto-remove packages.
