export function ValidationSafetyNotice() {
  return (
    <div className="panel-message">
      Safe validation uses limited non-destructive checks. It does not confirm exploitability. Scope must be authorised and manual validation is required.
    </div>
  )
}
