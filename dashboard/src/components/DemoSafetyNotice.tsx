export function DemoSafetyNotice() {
  return (
    <div className="demo-safety-notice">
      <strong>Local Demo Only</strong>
      <p>Portfolio Demo Mode uses fake localhost/demo targets, Redacted Demo Evidence, and simulated findings. It does not run scans or send live target requests.</p>
    </div>
  )
}

